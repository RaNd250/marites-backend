name: OSV-Scanner

on:
  schedule:
    - cron: "19 11 * * 1"
  push:
    branches: ["main"]
    paths:
      - "**/*"
      - "!.github/**" # Important: Exclude PRs related to .github from auto-run
      - "!.github/workflows/**" # Important: Exclude PRs related to .github/workflows from auto-run
      - "!.github/actions/**" # Important: Exclude PRs related to .github/actions from auto-run
  pull_request:
    branches: ["main"]
    paths:
      - "**/*"
      - "!.github/**" # Important: Exclude PRs related to .github from auto-run
      - "!.github/workflows/**" # Important: Exclude PRs related to .github/workflows from auto-run
      - "!.github/actions/**" # Important: Exclude PRs related to .github/actions from auto-run
  merge_group:
    branches: ["main"]

permissions:
  actions: read
  security-events: write
  contents: read

jobs:
  check_paths:
    uses: ./.github/workflows/check_paths.yml

  scan-scheduled:
    needs: check_paths
    if: ( github.event_name == 'push' && needs.check_paths.outputs.githubfolder == 'false' ) || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@8e5cf47b818121e8b405931c82126c2630b0b20d" # v2.3.8
    with:
      # Don't fail on push to main / scheduled runs: vulnerabilities are still
      # reported via the SARIF upload, but the CI run stays green.
      fail-on-vuln: false
      scan-args: |-
        -r
        ./

  scan-pr:
    needs: check_paths
    if: ( github.event_name == 'pull_request' && needs.check_paths.outputs.githubfolder == 'false' ) || ( github.event_name == 'merge_group' && needs.check_paths.outputs.githubfolder == 'false' )
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@8e5cf47b818121e8b405931c82126c2630b0b20d" # v2.3.8
    with:
      # Fail on PR runs (catch vulns before merge), but don't fail merge_group runs.
      fail-on-vuln: ${{ github.event_name != 'merge_group' }}
      scan-args: |-
        -r
        ./
